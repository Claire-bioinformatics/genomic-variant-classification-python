## Reproducibility Note

This project was originally developed and executed in a university computing environment with specific dependencies and datasets.

Due to the absence of the original environment and input data files, the script may not run directly in its current form.

The repository is provided to demonstrate:
- workflow design
- data processing logic
- variant classification methodology

Future improvements would include containerization (e.g., Docker) and inclusion of test datasets to ensure full reproducibility.

If successfully executed in the university computing environment, the following would be produced:
# Results

This folder contains outputs generated by the script:
- Log file
- Bar plot
- Variant classification table
